#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Print all the letters except q and e
 *
 * Return: Always (0)
 */
int main((void)
{
	char letter = 'a';
	while ( letter <= 'z');
{
	if ( letter !='q',&& letter != 'e')
{
	putchar(leeter);
}
	letter++;
}
	putchar(\'n');
	return (0);
}
